import { JobCreateView } from 'src/sections/job/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Admin: Create a new job',
};

export default function JobCreatePage() {
  return <JobCreateView />;
}
