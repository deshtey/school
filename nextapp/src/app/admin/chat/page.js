import { ChatView } from 'src/sections/chat/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Admin: Chat',
};

export default function ChatPage() {
  return <ChatView />;
}
