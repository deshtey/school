import { SchoolListView } from 'src/sections/school/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Admin: School List',
};

export default function SchoolListPage() {
  return <SchoolListView />;
}
