import { PostCreateView } from 'src/sections/blog/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Admin: Create a new post',
};

export default function PostCreatePage() {
  return <PostCreateView />;
}
